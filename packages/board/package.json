{
  "name": "espg-board",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "espruino": "^0.1.40",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  },
  "description": "",
  "scripts": {
    "clean": "rm -rf dist/",
    "build:dev": "yarn clean && tsc -p tsconfig.dev.json",
    "build:prod": "yarn clean && tsc -p tsconfig.prod.json"
  },
  "dependencies": {
    "@types/espruino": "^1.94.6",
    "@types/node": "^17.0.10"
  }
}
